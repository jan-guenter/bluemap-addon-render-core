# Notice

The production class first appears in the owner's MIT-licensed BlueMap
Sophisticated Add-on at commit
`8cdb778db3c123aec472404fdafecde28928e4b7`. Its frozen release snapshot is
commit `a75b1d82c3987fa9360a1e8a5910eedf90aca7cb`.

The module changes only the package and the visibility of the class, `sample`
method, and nested `Sample` record. Executable statements, parameters, record
components, neighbor selection, transforms, and maximum-light behavior are
unchanged.

[`provenance/origins.json`](provenance/origins.json) records the immutable
origin, transformation, BlueMap ABI, and seven matching portfolio consumers.
The frozen source under `src/origin/java` is a byte-exact build oracle and is
absent from publication archives.
